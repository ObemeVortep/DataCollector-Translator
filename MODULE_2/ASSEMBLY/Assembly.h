/*
 * Defines prototypes for system call wrappers using dynamic syscall numbers.
 * Each function declaration corresponds to a low-level NT API, allowing
 * interaction with processes, memory, registry, and files without standard API layers.
 * The syscall numbers are provided separately and mapped at runtime.
 *
 * Code has been redacted for ethical and security reasons.
 * 147 LOC
 */