/*
 * Implements a minimal file-saving routine using native NT system calls.
 * The file is created and written using `NtCreateFile` and `NtWriteFile`,
 * bypassing high-level API layers. File paths are prepared in NT-native format.
 *
 * Code has been redacted for ethical and security reasons.
 * 30 LOC
 */