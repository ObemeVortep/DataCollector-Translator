/*
 * Declares structures and function prototypes necessary for reflective DLL loading.
 *
 * Functionality includes:
 * - Definition of custom syscall function pointer types for memory allocation, protection, and cache flushing.
 * - `REFLECTIVE_FUNCTIONS` structure used to pass syscall function pointers and runtime parameters.
 * - Typedefs for PE relocation data to support manual DLL loading.
 * - Utilities for dereferencing memory at various integer sizes via macros.
 * - Forward declaration of `StartProgramm` entry point used in manual mapping contexts.
 *
 * Code has been redacted for ethical and security reasons.
 * 44 LOC
 */