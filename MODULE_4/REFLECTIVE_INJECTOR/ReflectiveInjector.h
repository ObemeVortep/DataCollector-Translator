/*
 * Declares a class responsible for reflective DLL injection into remote processes.
 * The implementation coordinates memory allocation, parameter marshalling, and cleanup.
 * It manages both local and remote buffer preparations and ensures safe transfer of the payload and execution context.
 *
 * Code has been redacted for ethical and security reasons.
 * 22 LOC
 */