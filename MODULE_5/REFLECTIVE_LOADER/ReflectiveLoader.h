/*
 * Declares types and macros for reflective DLL loading and manual memory manipulation.
 *
 * - Includes typedefs for NT memory-related syscalls and a structure to group their pointers.
 * - Provides function pointer type for DLL entry points and helper dereference macros.
 * - Defines image relocation format used for processing PE relocations during reflective loading.
 * - Serves as a foundational header for executing modules in-memory without standard loaders.
 *
 * Code has been redacted for ethical and security reasons.
 * 44 LOC
 */